##### 题目

返回两个数组合并后的中位数；

- 时间复杂度限制为 $O(log(m+n))$；
- 两个数组本身已为升序排序；



##### 解答

- 数组长度已知 → 中位数索引已知；
- 数组本身有序 → 双指针 谁小谁往前移；

~~~cpp
double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2)
{
    
}
~~~

